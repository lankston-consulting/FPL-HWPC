name: Lint

on:
  push:
    branches: 
      - "*"
    paths:
      - "**.py"
  pull_request:
    branches:
      - "main"
      - "prod-"
    paths:
      - "**.py"

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: psf/black@stable